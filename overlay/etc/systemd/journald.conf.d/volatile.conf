[Journal]
# RAM-only journal — required for read-only root, and avoids SD/USB write wear.
# Logs leave the box via the configured shipper (see packages/logging-*.list).
Storage=volatile
RuntimeMaxUse=64M
ForwardToSyslog=no
