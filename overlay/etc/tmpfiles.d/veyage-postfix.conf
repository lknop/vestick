# /var/lib/postfix is on tmpfs (see build.sh write_fstab). After each
# mount the dir is owned by root, but postfix expects postfix:postfix.
# systemd-tmpfiles-setup runs at boot to fix ownership before postfix.service.
d /var/lib/postfix 0755 postfix postfix -
