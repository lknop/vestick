# Allow root to log in over SSH with the password set by veyage-firstboot.
# Modern OpenSSH defaults to "prohibit-password" (key-only); standard
# Proxmox installer images flip this to "yes" because the web UI's noVNC
# console relies on it and most operators expect to be able to ssh in
# right after install.
PermitRootLogin yes
