# VEyage overlayroot configuration.
# tmpfs: upper layer is a RAM-only tmpfs — all writes vanish on reboot.
# This is the right default for the appliance use case; persistence comes
# from a separate RW partition that we'll wire up in Phase 3.
overlayroot="tmpfs"
overlayroot_cfgdisk="disabled"
